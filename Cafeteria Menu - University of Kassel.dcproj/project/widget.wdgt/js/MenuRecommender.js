function MenuRecommender() {
  this.preprocessors = new Array();
  
  this.preprocessors[] = new
  this.preprocessors[] = new StopWordProcessor(); // removes stop words like "auf", "an"
}

MenuRecommender.prototype.recommendMenu(menus) {
  // loop through all menus
  for (var i = 0; i < menus.length; i++) {
    // get cosinus
  }
}